# VRC Social Manager エージェントガイド

## 基本方針

- ユーザー向けの説明、ドキュメント、コードコメントは日本語で記述する。
- TypeScript/React、Rust、Tauri の既存規約を尊重し、必要以上に複雑な実装を避ける。
- `src/bindings.ts` は `tauri-specta` の生成物であるため、手動編集しない。
- 認証 Cookie、パスワード、OTP、VRChat の個人データをリポジトリやログへ保存しない。
- 作業開始時に既存の未コミット変更を確認し、依頼と無関係な変更を上書きしない。

## プロジェクト知識ベース

このリポジトリは、LLM が継続的に保守する Markdown Wiki を `knowledge/` に持つ。
詳しい構造と人間向けの使い方は [`knowledge/README.md`](knowledge/README.md)、
現在のページ一覧は [`knowledge/wiki/index.md`](knowledge/wiki/index.md) を参照する。

### 情報の三層

1. **一次情報**: ソースコード、設定、テスト、既存のプロジェクト文書。現在の実装については常にこれを最優先する。
2. **Raw sources**: `knowledge/raw/` に追加される外部仕様、調査資料、議事録など。取り込み後は内容を変更・削除しない。
3. **Wiki**: `knowledge/wiki/` の LLM 管理文書。一次情報と raw sources を統合した、検索しやすい派生資料として更新する。

Wiki と実装が矛盾する場合は実装を正とし、同じ作業内で Wiki を修正する。外部仕様と実装の差は、どちらかを無言で消さず明示する。

### 作業開始時

- 横断的な変更、調査、設計判断を伴う作業では、最初に `knowledge/wiki/index.md` を読む。
- 索引から関連ページだけを選んで読み、必ずコードや設定の一次情報で再確認する。
- 小さな局所修正では Wiki 全体を読む必要はない。

### Wiki を更新する条件

次のいずれかに該当する場合、コード変更と同じ作業で関連ページ、索引、ログを更新する。

- アーキテクチャ、データフロー、認証、永続化、リリース手順が変わる。
- 複数ファイルにまたがる重要な仕様や制約が判明する。
- 新しい raw source を取り込む。
- 再利用価値のある調査結果、比較、設計判断をユーザーが Wiki に残すよう求める。

表記修正、依存関係の機械的更新、局所的なバグ修正など、Wiki の説明を変えない作業では更新しない。

### Ingest 手順

1. 受領した資料を `knowledge/raw/YYYY-MM-DD-短い名前.拡張子` として追加する。元ファイル名や日付が明確な場合はそれを優先する。
2. raw source は追記後に編集・要約・整形しない。訂正版は別ファイルとして追加し、旧版との関係を Wiki に記す。
3. 資料を読み、既存 Wiki と一致する点、追加情報、矛盾、不明点を整理する。
4. 関連する既存ページを更新する。適切なページがなければ `knowledge/wiki/templates/page.md` を基に作成する。
5. `knowledge/wiki/index.md` にページへのリンクと一行要約を反映する。
6. `knowledge/wiki/log.md` の末尾へ `## [YYYY-MM-DD] ingest | 資料名` 形式で記録する。

### Query・調査手順

1. `knowledge/wiki/index.md` から関連ページを特定する。
2. Wiki の記述を、リンクされたコード、設定、raw source で検証する。
3. 回答では根拠となるリポジトリ内ファイルを示す。
4. 再利用価値があり、ユーザーの依頼範囲に含まれる成果だけを Wiki に保存する。
5. 保存した場合は索引を更新し、`## [YYYY-MM-DD] query | 主題` をログ末尾へ追記する。

### Lint 手順

Wiki の健全性確認を求められた場合は、少なくとも次を確認する。

- 存在しない相対リンクと、索引に載っていないページ
- Wiki と現在のコード・設定の矛盾
- 新しい情報に置き換えられた古い記述
- 根拠のない断定、出典の不足、未解決の矛盾
- 重要だが専用ページを持たない概念
- 秘密情報、認証情報、不要な個人情報の混入

修正内容を関連ページと索引へ反映し、`## [YYYY-MM-DD] lint | 対象` をログ末尾へ追記する。

### Wiki の記述規約

- ファイル名は英小文字の kebab-case、本文とタイトルは日本語を基本とする。
- 各ページに `title`、`kind`、`status`、`updated`、`sources` の YAML frontmatter を付ける。
- `status` は `verified`、`inferred`、`draft`、`stale` のいずれかとする。
- 事実には、可能な限り Markdown の相対リンクで一次情報を添える。
- 推測は「推測」、未確認事項は「未確認」と明記する。
- 矛盾は警告として残し、確認できるまで一方の主張に統合しない。
- ページ間リンクは GitHub でも読める通常の Markdown リンクを使う。
- `knowledge/wiki/log.md` の既存エントリは変更・並べ替えせず、末尾への追記だけを行う。
- 秘密情報や個人データは要約であっても記録しない。

## 検証

- フロントエンド変更では `pnpm run lint` と、必要に応じて `pnpm run build:vite` を実行する。
- Rust 変更では `cargo check --manifest-path src-tauri/Cargo.toml` を実行する。
- Wiki 変更では相対リンク、索引、frontmatter、ログの整合性を確認する。
- 実行できなかった検証や既知の失敗は、完了報告で明示する。
